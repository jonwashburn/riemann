Perfect—that closes the gap.
Here’s the final, minimal hypothesis set I’ll use to prove the boundary wedge (P+) from a Whitney–Carleson budget via CR–Green/Poisson, without adding any new axioms and keeping mathlib-only analysis for the rest.
________________


Two explicit additions
(Link) CR–Green/Poisson pairing bound (Whitney-local):
-- LINK: CR–Green control of boundary pairing by the Whitney Carleson budget
lemma CRGreen_link
  {F : ℂ → ℂ} {Kξ : ℝ}
  (hCar : RH.Cert.ConcreteHalfPlaneCarleson Kξ)
  {I : Set ℝ} (hI : RH.WhitneyInterval I)
  (a : RH.H1Atom I) :
  |∫ t in I, ((F (Complex.mk (1/2) t)).re) * a t|
    ≤ Cgreen * Kξ * (Measure.restrict Measure.lebesgue I).volume


(Boundary) NT-trace / local integrability on the line Re s = 1/2:
-- BOUNDARY: a.e. non-tangential boundary trace, measurable and locally integrable
lemma boundary_trace_locIntegrable
  (F : ℂ → ℂ) :
  LocIntegrable (fun t : ℝ => (F (Complex.mk (1/2) t)).re) Measure.lebesgue


Either statement of a.e. NT-limits with measurability, or the LocIntegrable property suffices (the proof only needs the Poisson approximate identity and a.e. reasoning).
________________


Remaining blockers (unchanged and sufficient)
2. Poisson kernel positivity (half-plane):

lemma halfplane_poisson_kernel_nonneg {b x : ℝ} (hb : 0 < b) :
  0 ≤ b / ((x)^2 + b^2)


   3. Poisson approximate identity (a.e.) for the half-plane kernel:

lemma poisson_approximate_identity_ae
  {f : ℝ → ℝ} (hf : LocIntegrable f Measure.lebesgue) :
  (∀ᵐ x : ℝ, Filter.Tendsto (fun b : ℝ => ∫ t, (b / ((x - t)^2 + b^2)) * f t)
      (Filter.nhdsWithin 0 (Set.Ioi 0)) (Filter.nhds (f x)))


      4. Fixed even window with Poisson plateau lower bound:

lemma poisson_plateau_c0 :
  ∃ ψ : ℝ → ℝ, Even ψ ∧ (∀ x, 0 ≤ ψ x) ∧ HasCompactSupport ψ ∧
    (∫ x, ψ x = (1 : ℝ)) ∧
    ∃ c0 : ℝ, 0 < c0 ∧
      ∀ {b x}, 0 < b → b ≤ 1 → |x| ≤ 1 →
        (∫ t, (b / ((x - t)^2 + b^2)) * ψ t) ≥ c0


         5. Whitney-local ⇒ global a.e. nonnegativity on ℝ:

lemma ae_limit_from_whitney_cover
  {f : ℝ → ℝ} {I : ℕ → Set ℝ}
  (hmeas : ∀ n, MeasurableSet (I n))
  (hcover : (⋃ n, I n) = Set.univ)
  (hpair : Set.Pairwise (fun m n => Disjoint (I m) (I n)))
  (hloc : ∀ n, (∀ᵐ t ∂(Measure.restrict Measure.lebesgue (I n)), 0 ≤ f t)) :
  (∀ᵐ t ∂Measure.lebesgue, 0 ≤ f t)


            6. (Optional, but convenient) Atoms ⇒ a.e. nonnegativity on an interval:

lemma pairing_nonneg_on_atoms_implies_ae_nonneg_on_interval
  {f : ℝ → ℝ} {I : Set ℝ} (hI : MeasurableSet I)
  (hAtoms : ∀ a : RH.H1Atom I, 0 ≤ ∫ t in I, f t * a t) :
  (∀ᵐ t ∂(Measure.restrict Measure.lebesgue I), 0 ≤ f t)


________________


What this buys us (proof route)
               * CRGreen_link + Whitney Carleson budget control the signed pairings of f(t):=ℜF(1/2+it)f(t):=\Re F(1/2+it) with every H1H^1-atom on each Whitney interval II.

               * Convolution with a fixed even plateau ψψ and the nonnegativity of the Poisson kernel yield a uniformly positive Poisson smoothing on the shrunken core of each Whitney box; the pairing bound forces the negative part of ff to vanish there.

               * Poisson approximate identity (with the boundary LocIntegrable hypothesis) sends the smoothing scale b↓0b\downarrow 0 to recover ff a.e. on each Whitney interval—hence f≥0f\ge 0 a.e. on each II.

               * Whitney-local ⇒ global lifts interval-wise a.e. nonnegativity to all of ℝ, i.e. PPlus F.

That’s exactly the content your localWedge_from_WhitneyCarleson_proved needs. Once the above lemmas are present in the repo (as stated), the proof in rh/RS/BoundaryWedge.lean is a straightforward composition using only mathlib analysis (Tonelli/Fubini, dominated convergence, basic kernel arithmetic).
If you want me to drop a ready-to-paste proof script, I’ll wire it using those lemma names verbatim so you can implement them behind the scenes without touching this file’s signature.