## Response to Referee Consolidated Report (Step 3) — Proof & Computation Blockers

Dear Referee (Dr. Amir Rahnama),

Thank you for the careful, concrete audit in `referee_step3.tex`. We agree with the substance of your “main blockers”: our prior revision’s narrative (“half-plane Schur on an open half-plane”) did not match what was actually certified by the shipped artifacts (a compact rectangle check plus an omitted tail estimate), and the rectangle regeneration must be independently reproducible.

Below we summarize the corrective actions taken in the **v3 revision** and how they address your items A2/B1–B3.

### A2. Rectangle artifact regeneration failed on Windows

We treat this as a serious reproducibility blocker.

#### What we changed
- **One-shot command line**: in `paper1_farfield_v3.tex` Appendix A we now run `theta_certify` with `--theta-time-limit 0` (no resume) and `python -u` to avoid buffered output.
- **Environment metadata recorded in artifacts**: `verify_attachment_arb.py` now emits a small `env` block in JSON artifacts (`python`, `python-flint`, platform, precision) so an auditor can immediately see if they are running on the intended runtime.

#### What we recommend the referee check
- Ensure the audit run matches these parameters exactly:
  - `--arith-gauge outer_zeta_proj`
  - `--outer-mode midpoint`
  - `--arith-det2-mode trunc`
  - `--theta-min-sigma-width 1e-4`, `--theta-min-t-width 1e-3`
  - `--prec 260`
  - `--theta-time-limit 0` (important: do not rely on resume unless *all* parameters match)

If a Windows build still produces `theta_not_lt_one_min_width` at points where the shipped artifact certifies `theta_hi < 1`, the most likely causes are (i) a script/version mismatch, (ii) a resume/parameter mismatch, or (iii) significantly wider enclosures on that platform that require higher precision / finer minimum widths.

### B1. Logical mismatch (“Schur on an open half-plane” vs. what is certified)

We agree. The prior “hybrid half-plane” language was stronger than the certified inputs.

#### What we changed
In `paper1_farfield_v3.tex` we **remove the half-plane reduction** and instead state/prove only what is fully covered by the audited computations:

- **Revised main statement (v3)**: `paper1_farfield_v3.tex` now certifies zero-freeness on the compact low-height region  
  \(R=[0.6,0.999]\times[-20,20]\).

This eliminates the uncovered region \(\{\sigma\ge 0.7,\ |t|\le 20\}\) as a logical gap because it is now *explicitly* included in the certified cover of \(R\).

### B2. “Large-height asymptotics” placeholder

We agree that the prior placeholder was not acceptable for a referee-proof claim.

#### What we changed
In v3 we **remove the large-height step entirely** by narrowing the proven statement to the low-height compact region \(R\) above. No omitted asymptotic lemma is invoked.

### B3. Unbounded-domain implication / patching

We agree that the prior draft’s unbounded-domain implication was not justified as written.

#### What we changed
In v3 there is **no unbounded-domain step**: the certified cover is on a compact set, and the argument is a direct combination of:
- a certified Schur bound for a Cayley field on \(R_{\mathrm L}=[0.6,0.7]\times[-20,20]\), and
- a certified \(\zeta\)-nonvanishing check on \(R_{\mathrm R}=[0.7,0.999]\times[-20,20]\).

### New artifact to fill the previously missing region

We add a direct, gauge-free artifact:
- `zeta_certify_sigma07_0999_t0_20.json`  
generated by the new verifier mode `--zeta-certify` (implemented in `verify_attachment_arb.py`).

This is a pure interval-arithmetic statement: on every certified box in the cover, the computed enclosure for \(\zeta(s)\) does not contain \(0\).

### Files delivered in this revision

- `paper1_farfield_v3.tex` / `paper1_farfield_v3.pdf` (revised statement + revised audit appendix)
- `verify_attachment_arb.py` (adds `--zeta-certify` and embeds `env` metadata into artifacts)
- `zeta_certify_sigma07_0999_t0_20.json` (new certified artifact)

Sincerely,  
Jonathan Washburn

